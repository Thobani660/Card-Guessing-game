<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Card Guessing Game</title>
    <link rel="stylesheet" href="/css/style.css"> <!-- Make sure styles.css exists in /public/css -->
</head>
<body>
    <%- include('partials/header') %> <!-- This includes the header from partials -->

    <!-- Game board container -->
    <div id="game-board" class="grid">
        <% for (let i = 0; i < 36; i++) { %>
            <div class="card" data-id="<%= i %>" onclick="flipCard(this)">
                <div class="card-front"></div>
                <div class="card-back">?</div>
            </div>
        <% } %>
    </div>

    <!-- Win message -->
    <div id="win-message" class="hidden">
        <h2>Congratulations! You've won the game!</h2>
        <button onclick="resetGame()">Play Again</button>
    </div>

    <script src="/js/game.js"></script> <!-- Link to your JavaScript file in /public/js -->
</body>
</html>
